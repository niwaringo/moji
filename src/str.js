module.exports = {
    /**
     * @param {string} str
     * @return {string}
     */
    trim(str) {
        return str.trim();
    },

    replace(str, pattern, replacement) {
        return str.replace(pattern, replacement);
    },
};
